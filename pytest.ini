[pytest]
# Match pytest-asyncio's forthcoming default explicitly so fixture lifecycle
# behavior does not change silently when the dependency is upgraded.
asyncio_default_fixture_loop_scope = function
