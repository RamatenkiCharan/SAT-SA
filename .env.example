# Copy to .env and replace every placeholder before using Docker.
POSTGRES_PASSWORD=replace-with-a-long-random-password
SAT_SECRET_KEY=replace-with-a-64-character-random-secret

# Optional local demo/test bootstrap. Leave disabled for a durable deployment.
# SAT_SEED_DEMO_USERS=true
# SAT_BOOTSTRAP_ADMIN_PASSWORD=choose-a-local-demo-admin-password
# SAT_BOOTSTRAP_SUPERVISOR_PASSWORD=choose-a-local-demo-supervisor-password
# SAT_BOOTSTRAP_ANALYST_PASSWORD=choose-a-local-demo-analyst-password
# SAT_SEED_DEMO_DATA=true
